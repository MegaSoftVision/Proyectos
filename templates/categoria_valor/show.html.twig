{% extends 'base.html.twig' %}

{% block title %}CategoriaValor{% endblock %}

{% block body %}
    <h1>CategoriaValor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoria_valor.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ categoria_valor.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categoria_valor_index') }}">back to list</a>

    <a href="{{ path('categoria_valor_edit', {'id': categoria_valor.id}) }}">edit</a>

    {{ include('categoria_valor/_delete_form.html.twig') }}
{% endblock %}
