<br><br>
  <form method="post" action="{{ path('encuesta_delete', {'id': encuesta.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ encuesta.id) }}">
    <button class="btn btn-danger">Borrar Encuesta</button>
	</form>


