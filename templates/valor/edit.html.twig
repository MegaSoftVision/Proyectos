{% extends 'base.html.twig' %}

{% block title %}Edit Valor{% endblock %}

{% block body %}
    <h1>Edit Valor</h1>

    {{ include('valor/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('valor_index') }}">back to list</a>

    {{ include('valor/_delete_form.html.twig') }}
{% endblock %}
